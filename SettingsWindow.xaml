<Window x:Class="ShipVision.SettingsWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        Title="Settings" Height="200" Width="400">
    <Grid Margin="10">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto" />
            <RowDefinition Height="Auto" />
            <RowDefinition Height="*" />
        </Grid.RowDefinitions>

        <TextBlock Text="AIS Provider:" VerticalAlignment="Center" />
        <ComboBox x:Name="ProviderComboBox" Grid.Row="0" Margin="120,0,0,0" Width="200">
            <ComboBoxItem Content="aisstream.io" />
        </ComboBox>

        <TextBlock Text="API Key:" Grid.Row="1" VerticalAlignment="Center" />
        <TextBox x:Name="ApiKeyTextBox" Grid.Row="1" Margin="120,0,0,0" Width="200" />

        <Button Content="Save" Grid.Row="2" HorizontalAlignment="Right" Width="80" Margin="0,10,0,0" Click="SaveButton_Click" />
    </Grid>
</Window>